/**
 * Reflects the data structure required for simple [ngx-charts](https://github.com/swimlane/ngx-charts) bar charts
 */
export interface ChartItemValue {
  /**
   * Name of the data point
   */
  name: string;
  /**
   * Value of the data point
   */
  value: number;
}
